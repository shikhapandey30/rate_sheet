json.extract! program, :id, :loan_purpose, :loan_size, :loan_type, :term, :interest_rate, :lock_period, :fannie_mae, :base_rate, :adjustment_rate, :created_at, :updated_at
json.url program_url(program, format: :json)
